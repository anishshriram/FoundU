// Implemented in Milestone 2 — Authentication Service
//
// Responsibilities:
// - registerUser: validate Rutgers .edu email, hash password, call SMS provider,
//   create user record, issue JWT
// - loginUser: verify credentials, issue JWT
// - logoutUser: no-op server-side (stateless JWT — client deletes from Keychain)

export {}
