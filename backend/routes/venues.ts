import { FastifyPluginAsync } from 'fastify'

// Implemented in Milestone 9 — Venue Service
//
// Protected by auth middleware.
//   GET /venues — returns active venues, optionally filtered by category
const venuesRoutes: FastifyPluginAsync = async (_app) => {
  // Routes registered in Milestone 9
}

export default venuesRoutes
